{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Delete Directory",
    "description": "Delete a directory.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to delete the directory or not.",
            "properties": {
                "status": {
                    "type": "boolean",
                    "description": "Whether the directory was deleted successfully."
                },
                "message": {
                    "type": "string",
                    "description": "Message indicating the result of the operation."
                }
            }
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}
