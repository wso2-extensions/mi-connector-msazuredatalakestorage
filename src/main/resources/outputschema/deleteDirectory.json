{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Delete Directory",
    "description": "Delete a directory.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to delete the directory or not."
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}