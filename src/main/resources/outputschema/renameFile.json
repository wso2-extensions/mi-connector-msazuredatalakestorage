{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Rename File",
    "description": "Rename a file.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to rename the file or not."
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}