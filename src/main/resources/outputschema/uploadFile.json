{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Upload File",
    "description": "Upload a file.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to upload the file or not."
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}