{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Update Metadata",
    "description": "Update metadata for a file.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to update the metadata or not.",
            "properties": {
                "status": {
                    "type": "boolean",
                    "description": "Whether the metadata was updated successfully."
                },
                "message": {
                    "type": "string",
                    "description": "Message indicating the result of the operation."
                }
            }
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}
