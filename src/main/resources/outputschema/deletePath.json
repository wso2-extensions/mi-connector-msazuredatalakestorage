{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Delete Path",
    "description": "Delete a path.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to delete the path or not."
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}