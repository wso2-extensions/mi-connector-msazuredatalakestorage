{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Rename Directory",
    "description": "Rename a directory.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to rename the directory or not."
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}