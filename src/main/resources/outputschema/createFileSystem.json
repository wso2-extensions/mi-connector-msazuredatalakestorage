{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Create File System",
    "description": "Create a new file system.",
    "properties": {
        "payload": {
            "type": "object",
            "description": "Whether to create the file system or not.",
            "properties": {
                "status": {
                    "type": "boolean",
                    "description": "Whether the file system was created successfully."
                },
                "message": {
                    "type": "string",
                    "description": "Message indicating the result of the operation."
                }
            }
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}
