{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Create File System",
    "description": "Create a new file system.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to create the file system or not."
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}
