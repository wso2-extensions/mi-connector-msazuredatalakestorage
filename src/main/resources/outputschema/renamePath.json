{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Rename Path",
    "description": "Rename a Path.",
    "properties": {
        "payload": {
            "type": "object",
            "description": "Whether to rename the path or not.",
            "properties": {
                "status": {
                    "type": "boolean",
                    "description": "Whether the path was renamed successfully."
                },
                "message": {
                    "type": "string",
                    "description": "Message indicating the result of the operation."
                }
            }
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}
