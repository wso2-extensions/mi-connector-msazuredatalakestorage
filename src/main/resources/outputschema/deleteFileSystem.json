{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Delete File System",
    "description": "Delete a file system.",
    "properties": {
        "payload": {
            "type": "object",
            "description": "Whether to delete the file system or not.",
            "properties": {
                "status": {
                    "type": "boolean",
                    "description": "Whether the file system was deleted successfully."
                },
                "message": {
                    "type": "string",
                    "description": "Message indicating the result of the operation."
                }
            }
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}
