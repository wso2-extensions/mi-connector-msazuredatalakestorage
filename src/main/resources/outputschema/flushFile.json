{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Flush File",
    "description": "Flush a file.",
    "properties": {
        "payload": {
            "type": "boolean",
            "description": "Whether to flush the file or not."
        }
    },
    "required": [
        "payload"
    ],
    "additionalProperties": false
}